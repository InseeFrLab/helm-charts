apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opa.fullname" . }}-test-connection"
  labels:
    {{- include "opa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
